pipeline {
    agent any
    stages {        
        stage('Docker Build') {
            steps {
                powershell (script: 'docker-compose')
                powershell (script: 'docker images -a')
            }
        }
    }
}